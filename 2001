#include <iostream>
#include <stack>

using namespace std;

//--------СТЕК---------------

void PrintStack(stack <int> st)
{
	while (!st.empty())
	{
		cout << st.top()<<" ";
		st.pop();
	}
}

//распечатать в прямом порядке
stack <int> ReversedPrintStack(stack <int> st)
{

	return st1;
}

int main()
{
	stack <int> st;
	int x=1;
	while (x!=0)
	{
		cin >> x;
		st.push(x);
	}
	PrintStack(st);
}

//вводим стек пока не 0 и распечатываем
