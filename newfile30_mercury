#include <iostream>
#include <map>
#include <vector>
#include <string>
#include <set>


//КОНТЕЙНЕР map

using namespace std;

//int main()
//{
//	map <int, string> m;
//}

//int main() 
//{
//	setlocale(LC_ALL, "RUSSIAN");
//	int n;
//	cin >> n;
//	map <long, long> m;
//	for (long i = 0; i < n; ++i) {
//		long x;
//		cin >> x;
//		++m[x];
//	}
//	if (n == 0) {
//		cout << "не найдено";
//		return 0;
//	}
//	pair<long, long>max = make_pair(m.begin()->first, m.begin()->second);
//	for (map <long, long>::iterator it = m.begin(); it != m.end(); ++it) {
//		if (max.second < it->second)
//			max = make_pair(it->first, it->second);
//	} cout << "значение " << max.first << " встречается " << max.second << " раз(а)" << endl;
//	system("pause");
//	return 0;
//}

//Во входной строке записана последовательность чисел через пробел. Для каждого числа выведите слово YES (в отдельной строке), если это число ранее встречалось в последовательности или NO, если не встречалось.
//int main()
//{
//	setlocale(LC_ALL, "Rus");
//	int n;
//	cin >> n;
//	map <long, long> chsl;
//	for (int i = 0; i < n; i++)
//	{
//		long x;
//		cin >> x;
//
//		if (chsl.find(x) != chsl.end())
//		{
//			cout << "YES" << " ";
//		}
//		else
//		{
//			cout << "NO" << " ";
//		}
//		++chsl[x];
//	}
//	return 0;
//}

//3
//Анаграммы – это слова, у которых те же буквы в том же количестве, но они располагаются в другом порядке. Исключить из массива строк слова-анаграммы.
//в векторе могут храниться map'ы
//int main()
//{
//	setlocale(LC_ALL, "Rus");
//	int n;
//	cin >> n;
//	//vector <map<char,long>> pk;
//	//vector <string> wslova;
//	string slovo;
//	map<char, long>bukvi;
//	vector <pair<string, map<char,long>>> wslova;
//
//	for (int i = 0; i < n; i++)
//	{
//		cin >> slovo;
//		for (int j = 0; j < wslova.size(); j++)
//			char c = wslova[i][j];
//		wslova=make_pair(slovo, bukvi);
//	}
//	for (int i = 0; i < n; i++) {
//		bool f=0;
//		for (int j = i + 1; j < n; j++) {
//			if (wslova[i].second == wslova[j].second)
//			{
//				f = 1;
//			}
//		}
//		if (!f)
//		{
//			cout << wslova[i].first << " ";
//		}
//	}
//	cout << endl;
//	return 0;
//}


//map <char, int> KolSymb(string s)
//{
//map<char, int> w;
//for (char c:s)
//w[c]++; //w[c]=w[c]+1;
//return w;
//}
//int main()
//{
//	setlocale(LC_ALL, "Rus");
//	vector <string> words = { "ПАРК","КАРП","СОМ","МОС","ДЕНЬ" };
//	map <map<char, int>, int> s;
//	for (const string& word : words)
//		s[KolSymb(word)]++;
//	cout << "Слова без ..." << endl;
//for (const string & word : words)
//{
//	if (s[KolSymb(word)] == 1)
//		cout << word << endl;
//}
//return 0;
//}


/*
* map <char, int> KolSymb(string s)
* {
* map<char, int> w;
* for (char c:s)
* w[c]++; //w[c]=w[c]+1;
* return w;
* }
* int main()
* {
* vector <string> words={"здесб слова"};
* map <map<char,int>,int> s;
* for (const string & word : words)
* {
* if (s[KolSymb(word)]==1)
* cout<<word<<endl;
* }
* return 0;
* }
*/

//тоже самое но с палиндромами
//map <string, long> KolvoSymb(const vector<string>& words)
//{
//	map <char, long> w;
//	for (const string& : words)
//		w[c]++;
//	return w;
//}
//int main()
//{
//	setlocale(LC_ALL, "Rus");
//	vector <string> words;
//	for (const string & word)
//}

//bool check(const string& s)
//{
//	int forleft = 0;
//	int forright = s.size() - 1;
//	while (forleft < forright)
//	{
//
//	}
//}

//map <string, long> KolvoSymb(const vector<vector>& words)
//{
//	map<string, long> w;
//	for (const string& word : words)
//	{
//		bool f = 1;
//		int forleft = 0;
//		int forright = word.size() - 1;
//		while (forleft < forright)
//		{
//
//		}
//	}
//}

int main()
{
	setlocale(LC_ALL, "Rus");
	vector<string> words = { "ОНО","АННА","КАЗАК","ДОВОД","ПОТОП","РОТОР","ТОПОТ" };
	map <string, long> pal;

	for (const string& word : words)
	{
		bool f = 1;
		int lf = word.size();
		for (int i = 0; i < lf / 2; i++)
		{
			if (word[i] != word[lf - 1 - i])
			{
				f = 0;
			}
		}
		if (f)
		{

		}
	}
}
